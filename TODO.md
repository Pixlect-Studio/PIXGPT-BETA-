- [x] Fix Railway entry so `index.html` loads from Flask root
- [x] Add Flask routes to serve `index.html` at `/` and support front-end fetch endpoints

- [x] Ensure status check endpoint returns pretty printed response ("status": "ok")

- [ ] Run local test (`python app.py`) and verify `/` returns HTML
- [ ] Run simple curl check for `/` and `/api/message`
- [ ] Prepare Railway `Procfile` / instructions if missing (web: gunicorn ...)

